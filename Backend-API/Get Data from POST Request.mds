팁: GET 및 POST 외에도 몇 가지 다른 http 방법이 있습니다. 관례적으로 http 동사와 서버에서 실행하려는 작업 사이에는 대응 관계가 있습니다. 기존 매핑은 다음과 같습니다.

POST(때때로 PUT) - 요청과 함께 전송된 정보를 사용하여 새 리소스를 생성하고,

GET - 기존 리소스를 수정하지 않고 읽습니다.

PUT 또는 PATCH(경우에 따라 POST) - 전송된 데이터를 사용하여 리소스를 업데이트합니다.

DELETE => 리소스를 삭제합니다.

서버와의 연결을 협상하는 데 사용되는 몇 가지 다른 방법도 있습니다. GET을 제외하고 위에 나열된 다른 모든 메서드는 페이로드(예: 요청 본문에 데이터)를 가질 수 있습니다. 본문 파서 미들웨어는 이러한 메서드에서도 작동합니다.
